<%= form_with(model: user) do |form| %>
<%# form_for(@user, :url => { :action => "update" }) do |form| %>
  <% if user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
        <% user.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :admin %>
    <%= form.check_box :admin %>
  </div>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %> <br>

    <%= form.label :email %>
    <%= form.text_field :email %> <br>

    <%= form.label :password, "password (6 characters or more)", :autocomplete => "off"  %>
    <%= form.password_field :password, autocomplete: "new-password" %> <br>
    
    <%= form.label :password_confirmation, "retype password"  %>
    <%= form.password_field :password_confirmation, autocomplete: "new-password"  %> <br>

    <%= link_to "Change your password", edit_user_registration_path %>
  </div>
  
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

